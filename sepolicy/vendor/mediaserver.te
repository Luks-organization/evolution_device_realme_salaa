allow mediaserver media_quality_service:service_manager find;
allow mediaserver vendor_default_prop:file { read };
allow mediaserver vendor_variant_prop:file { read };

get_prop(mediaserver, vendor_variant_prop)
get_prop(mediaserver, vendor_default_prop)
